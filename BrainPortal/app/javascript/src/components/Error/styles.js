import styled from "styled-components";
import { Box } from "src/components/globals";

export const StyledErrorContainer = styled(Box).attrs(() => ({
  flex: "auto",
  justifyContent: "center",
  alignItems: "center",
  alignSelf: "stretch",
  position: "absolute",
  width: "100%",
  height: "100%"
}))``;

export const StyledIcon = styled(Box).attrs(() => ({}))``;
